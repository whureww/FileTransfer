# 隐私策略 / Privacy Policy

**最后更新日期：2026-08-02**

本隐私策略适用于 臻传 Silex（以下简称"本程序"）—— 一个跨平台文件传输工具，包含 PC 客户端、Android 手机端和中继服务器三个组件。本程序尊重并保护用户的隐私，本策略说明本程序收集、使用和保护数据的方式。

## 一、数据收集范围

### 1.1 PC 客户端

本程序在 PC 端**仅收集以下必要数据**用于实现文件传输功能：

| 数据项 | 用途 | 存储位置 | 加密方式 |
|--------|------|----------|----------|
| 关闭行为偏好（关闭窗口时的选择：最小化/退出/询问） | 记住用户选择，避免重复询问 | Windows 注册表 `HKCU\Software\Silex\Silex` | 明文（`REG_DWORD`，非敏感数据） |
| 自定义中继服务器地址（IP 和端口，可选） | 连接用户自建的中继服务器 | Windows 注册表 `HKCU\Software\Silex\Silex` | **DPAPI 加密**（`REG_BINARY`） |
| 本机 IP 地址列表 | 局域网直连模式显示本机 IP 供对端连接 | 仅内存中，不持久化 | 不适用 |

### 1.2 Android 手机端

| 数据项 | 用途 | 存储位置 |
|--------|------|----------|
| 接收的文件 | 保存传输接收的文件 | 公共存储 `Silex/received/` 或应用专用目录 |
| 自定义中继服务器地址（可选） | 连接用户自建的中继服务器 | 应用内部存储 |

### 1.3 中继服务器

中继服务器作为转发节点，**不存储任何文件内容或用户数据**：
- 文件数据仅在内存中转发，传输完成立即释放
- 不记录传输日志、不保存文件元数据
- 仅在运行时维护房间配对状态（房间码 → 连接），传输完成即销毁

## 二、数据收集原则

### 2.1 最小化原则
本程序**不收集**以下数据：
- ❌ 个人身份信息（姓名、邮箱、手机号等）
- ❌ 设备硬件指纹或唯一标识符
- ❌ 地理位置
- ❌ 联系人、通讯录
- ❌ 浏览历史或使用习惯
- ❌ 崩溃日志或遥测数据

### 2.2 本地优先原则
- 所有用户数据均存储在**本地设备**，不上传到任何服务器
- 中继服务器仅转发文件传输流量，不收集任何用户信息
- 无云端账户系统，无需注册登录

## 三、数据传输与网络通信

### 3.1 传输模式与数据流向

| 传输模式 | 数据流向 | 是否经过服务器 | 加密状态 |
|----------|----------|----------------|----------|
| 局域网直连 | 手机 ↔ PC（同一 WiFi） | 否，点对点直连 | **明文传输**（TCP 流） |
| 房间码中继 | 手机 ↔ 中继服务器 ↔ PC | 是，仅转发 | **明文转发**（服务器不存储） |
| 扫码配对 | 同房间码中继 | 是，仅转发 | **明文转发** |

### 3.2 重要安全提示
⚠️ **本程序当前版本的文件传输不进行端到端加密**。文件以原始字节流方式传输，协议字段（魔数、版本、文件大小、文件名）也为明文。请在以下场景注意：
- 在**可信网络环境**（如家庭 WiFi）中使用局域网模式
- 在**不可信网络**中传输敏感文件时，建议先使用第三方工具加密文件再传输
- 中继服务器管理员理论上可看到经过的流量（但无法长期保存，因服务器不持久化任何数据）

### 3.3 网络权限说明

**Android 手机端申请的网络权限**：
- `INTERNET`：进行文件传输网络通信
- `ACCESS_NETWORK_STATE`：检测网络状态（仅用于显示连接信息）
- `ACCESS_WIFI_STATE`：局域网模式下获取 WiFi 状态

**Android 手机端申请的存储权限**：
- `MANAGE_EXTERNAL_STORAGE`（Android 11+）：用于在公共目录创建 `Silex/received/` 接收文件夹
- `READ/WRITE_EXTERNAL_STORAGE`（Android 10 及以下）：同上

**Android 手机端申请的摄像头权限**：
- `CAMERA`：用于扫描二维码（仅在用户主动点击"扫码接收"时启用，可随时在系统设置中撤销）

## 四、数据安全保护措施

### 4.1 敏感数据加密
- **自定义中继服务器地址**：使用 Windows DPAPI（Data Protection API）加密后存储到注册表
  - 加密数据仅当前 Windows 用户 + 当前机器可解密
  - 复制到其他用户或其他机器无效
  - 注册表中不存在明文 IP 地址

### 4.2 协议安全
- 文件名过滤：自动去除路径分隔符、Windows 非法字符，拦截目录穿越攻击和保留设备名
- 协议校验：魔数和版本号校验，防止误连接
- 磁盘空间预检：传输前检查接收端可用空间
- 中继服务器保护：连接数限制、房间数限制、传输超时、数据量上限

### 4.3 防火墙与端口
- PC 端启动时自动添加 Windows 防火墙入站规则（需 UAC 授权）
- 仅开放文件传输所需端口（9090 局域网 / 9091 中继）
- 不开放不必要的网络端口

## 五、第三方组件

本程序使用以下开源第三方组件，这些组件**不进行任何数据收集**：

| 组件 | 用途 | 许可证 |
|------|------|--------|
| Nayuki QR Code | 二维码生成 | 公开领域 |
| CameraX | Android 相机预览 | Apache 2.0 |
| ZXing | 二维码扫描识别 | Apache 2.0 |
| Jetpack Compose | Android UI | Apache 2.0 |
| Kotlin Coroutines | 异步编程 | Apache 2.0 |

## 六、数据删除与卸载

### 6.1 卸载程序
- **PC 端**：通过控制面板卸载后，程序文件将被删除。注册表中的用户设置（`HKCU\Software\Silex`）需手动清除，或使用卸载程序的"删除用户配置"选项
- **Android 端**：卸载应用后，应用专用目录中的数据将被自动删除。公共目录 `Silex/received/` 中的已接收文件需手动删除
- **清除自定义中继服务器地址**：在 PC 端"高级设置"中清空地址并确定，或删除注册表项 `HKCU\Software\Silex\Silex`
- **清除接收的文件**：手动删除接收目录中的文件

## 七、儿童隐私

本程序不针对 13 岁以下儿童，不会故意收集儿童的个人信息。如果发现有任何儿童向我们提供了个人信息，我们将及时删除。

## 八、隐私策略变更

本隐私策略可能随程序版本更新而调整。重大变更将在程序启动时的更新日志中提示用户。继续使用本程序即视为接受更新后的隐私策略。

## 九、开源与透明

本程序为开源软件，源代码托管在：`https://github.com/whureww/Silex`

任何人都可以审查源代码以验证本隐私策略的准确性。如发现与本策略不符的数据收集行为，欢迎在 GitHub 提交 Issue。

## 十、联系我们

如有隐私相关问题，请在 GitHub 仓库提交 Issue：
`https://github.com/whureww/Silex/issues`

---

**声明**：本程序不主动收集任何用户隐私数据，所有数据收集行为均在本策略中明示。本程序的设计理念是"本地优先、最小收集、用户可控"。
